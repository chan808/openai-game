# AGENTS.md

Codex/에이전트가 이 저장소에서 작업할 때 따르는 규칙.

## 프로젝트

탑다운 2D 액션 게임. 브라우저에서 바로 실행되는 웹 빌드를 유지한다.
제품 정의는 `PRD.md`, 기술 결정은 `docs/TECH_SPEC.md`를 먼저 읽을 것.

## 스택

Node.js 22.12+ · TypeScript (strict) · Vite · Phaser 4.2.1 · Vitest · 정적 웹 빌드

## 절대 규칙

1. **고정 타임스텝을 우회하지 않는다.** 모든 게임 로직은 `GameClock`이 공급하는 `dt`로만 진행한다. `Date.now()`, `performance.now()`, `requestAnimationFrame` 델타를 로직에서 직접 읽지 않는다.
2. **Phaser Arcade Physics를 켜지 않는다.** Phaser는 presentation 계층에서 렌더링·브라우저 입력·카메라·오디오·이펙트를 담당한다. 게임 상태, 이동, 충돌, 공격 판정과 시간 규칙은 `src/core`와 `src/game`의 순수 TypeScript로 처리한다.
3. **`src/core`와 `src/game`은 Phaser를 import하지 않는다.** 순수 TypeScript만. 렌더링·DOM·브라우저 입력 API 접근 금지. Phaser 의존 코드는 `src/presentation`에 두며, `src/main.ts`는 Phaser 부트스트랩과 의존성 조립만 할 수 있다.
4. **`Math.random()`을 사용하지 않는다.** 랜덤이 필요해지면 시드 기반 `Rng`를 추가한다.
5. **플레이어 입력은 `InputSource` 인터페이스를 통해서만 읽는다.** 키보드 이벤트를 액터 코드에서 직접 구독하지 않는다. (분신·co-op 확장의 전제)
6. **검증 전 시스템을 미리 만들지 않는다.** 첫 프로토타입은 이동·조준·기본 공격·죽지 않는 피격 대상만 구현한다. 키 배정, 슬로우, 순간이동, 장비, 적 AI는 사용자 플레이테스트 뒤 확정한다.
7. **네트워크 기능을 만들지 않는다.** API 키를 클라이언트 코드에 넣지 않는다.

## 코딩 컨벤션

- 파일명: 클래스는 `PascalCase.ts`, 그 외 `camelCase.ts`
- 함수는 하나의 명확한 책임을 유지하고, 필요할 때 의미 단위로 분리한다
- 주석은 코드를 반복하지 않는다. 비자명한 의도나 제약만 기록
- 플레이테스트로 조정할 게임필 수치는 `src/content/tuning.ts`에 모은다. 고정 틱과 catch-up 상한 같은 기술 상수는 담당 모듈에서 이름을 붙인다
- 미사용 import/변수를 남기지 않는다
- 프로덕션 코드에 `console.log`를 남기지 않는다

## 작업 방식

- 요청받은 것만 구현한다. 추측성 기능·미사용 추상화·불필요한 설정 옵션을 만들지 않는다
- 발생할 수 없는 상황에 대한 에러 처리를 넣지 않는다
- 기존 코드 스타일을 따른다. 요청 범위 밖을 리팩터링하거나 재포맷하지 않는다
- 커밋 메시지는 영문 Conventional Commits 형식 `<type>(optional-scope): <summary>`를 사용한다. 요약은 명령형 한 줄로 작성하고 마침표를 붙이지 않는다
- 커밋 타입은 `feat`(기능), `fix`(버그), `refactor`(동작 변화 없는 구조 개선), `test`(테스트), `docs`(문서), `chore`(유지보수), `build`(빌드·의존성), `ci`(CI) 중에서 선택한다
- `docs/DEVLOG.md`는 모든 커밋마다 수정하지 않는다. 기술 방향, 스코프나 플레이테스트처럼 굵직한 판단이 확정됐을 때만 실제 이유와 검증 결과를 3~5줄로 기록한다
- 변경 후 `npm run verify`를 실행해 테스트와 프로덕션 빌드가 모두 통과하는지 확인한다

## 스코프 경계

`PRD.md`의 "명시적으로 안 만드는 것" 목록을 지킨다. 그 항목이 필요해 보이면 구현하지 말고 먼저 지적할 것.
